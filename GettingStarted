Getting started
Set `ENABLE_MOCK_DATA=false` in `api/.env` to use the live Game Pass fetcher. Restart the FastAPI service (or hit `/refresh`) after updating your Twitch credentials and, optionally, `GAMEPASS_MARKET` / `GAMEPASS_LANGUAGE` to pull fresh catalog data directly from Microsoft.
